# Apps Agent Guide

This directory covers both `apps/frontend` and `apps/backend`.

When you make substantive changes under `apps/`, always update the matching:
- `daily-log/YYYY-MM-DD.md`
- relevant `handoff/` artifact(s)

Keep those records aligned with the actual code change and verification results.
