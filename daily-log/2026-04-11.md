# 2026-04-11

## 작업 내용
- 국세청 진위확인 실패 원인이 관리자 화면에서 전부 `국세청 진위확인 결과가 일치하지 않습니다.`로만 보이던 문제를 조사했다.
- `NationalTaxServiceClient`가 응답 문자열에서 `"valid":"01"` 존재 여부만 검사하고 있어 실제 `valid_msg`, `status_code`를 전혀 파싱하지 않는 점을 확인했다.
- 국세청 `/validate` 응답을 JSON 구조로 파싱하도록 수정했다.
  - `data[0].valid == "01"` 이면 성공
  - `data[0].valid == "02"` 이면 사업자 불일치 실패
  - `data[0].valid_msg`를 실패 메시지로 저장
  - `status.status` 계열 값 중 일부를 매칭 결과로 보존
- 국세청 에러 응답도 파싱하도록 수정했다.
  - `status_code == BAD_JSON_REQUEST`
  - `REQUEST_DATA_MALFORMED`
  - `TOO_LARGE_REQUEST`
  - `INTERNAL_ERROR`
  - `HTTP_ERROR`
  를 각각 의미 있는 `ApiException` 코드/메시지로 변환하도록 보강했다.
- 국세청 파싱 로직에 대한 단위 테스트 `NationalTaxServiceClientTest`를 추가했다.
  - 성공 응답 파싱
  - `valid_msg` 실패 메시지 파싱
  - 에러 응답 `status_code` 매핑
- 백엔드 `./gradlew test` 전체 통과를 확인했다.
- 수정 사항 반영을 위해 백엔드를 재기동했다.
- 실데이터 QA를 위해 `하삼동 커피 / 경기도 안양시 만안구 만안로 35`로 점주 신청을 직접 재현했다.
- 초기 QA 결과:
  - 사업자 자동 검증은 `AUTO_VERIFICATION_FAILED`
  - 카카오 검증은 같은 주소의 여러 업장 때문에 `FAILED`
  - 중복 신청도 그대로 추가 생성되는 문제를 확인했다.
- 카카오 exact-address-only 정책을 유지하면서도, 같은 실영업주소 결과가 여러 건일 때 상호명 정규화 일치로 유일한 매장을 좁히는 로직을 추가했다.
- 중복 진행 중 신청 방지를 위해 같은 점주 + 같은 사업자번호 + 같은 정규화 주소 + `PENDING/UNDER_REVIEW` 조합이면 생성/수정을 막도록 보강했다.
- 관리자 상세 응답에 지도 검증 `candidateCount`, `selectedJibunAddress`, `failureCode`를 추가해 운영자가 ambiguous 상황을 더 잘 볼 수 있게 했다.
- 점주 POS 화면에 `내 신청 현황` 섹션을 추가해 사업자 검증 상태와 지도 검증 상태를 스스로 확인할 수 있게 했다.
- 점주 신청 유틸을 레거시 `/store-applications` 대신 `/store-registration-requests` 경로로 정리했다.
- 모바일 로그인/웹 로그인에서 탭 전환 후 이전 계정 타입 에러가 남아 있던 문제를 수정했다.
- 수정 후 다시 `하삼동 커피 / 경기도 안양시 만안구 만안로 35`로 요청했을 때
  - `AUTO_VERIFICATION_FAILED`
  - `mapVerificationStatus = VERIFIED`
  - `verifiedStoreName = 하삼동커피 안양만안구청점`
  으로 개선된 것을 로컬 admin API에서 확인했다.
- 동일 요청을 즉시 다시 보냈을 때 `OWNER_APPLICATION_DUPLICATED`로 차단되는 것도 확인했다.

## 영향
- 이제 사업자 자동 검증 실패 시 관리자 상세 이력에서 더 구체적인 실패 사유를 볼 수 있다.
- 단순 불일치와 외부 API 요청 형식 오류/서버 오류를 구분할 수 있다.
- 동일 주소의 여러 카카오 결과 중 실제 상호명이 하나로 좁혀지는 경우는 자동 저장까지 진행할 수 있다.
- 점주가 관리자 승인 전에도 자기 신청이 어디서 막혔는지 POS 화면에서 확인할 수 있다.

## 수정 파일
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/backend/src/main/java/com/toggle/service/NationalTaxServiceClient.java`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/backend/src/test/java/com/toggle/service/NationalTaxServiceClientTest.java`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/backend/src/main/java/com/toggle/service/OwnerApplicationService.java`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/backend/src/main/java/com/toggle/repository/OwnerApplicationRepository.java`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/backend/src/main/java/com/toggle/dto/owner/MapVerificationHistoryResponse.java`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/backend/src/main/java/com/toggle/entity/MapVerificationHistory.java`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/backend/src/test/java/com/toggle/ToggleBackendApplicationTests.java`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/frontend/src/pages/Pos.jsx`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/frontend/src/pages/Pos.module.css`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/frontend/src/pages/AdminWeb.jsx`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/frontend/src/pages/Login.jsx`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/frontend/src/pages/LoginWeb.jsx`
- `/Users/gwon-ohhyeon/Desktop/toggle/apps/frontend/src/lib/owner.js`

## 다음 할 일
- 실제 사업자 정보로 다시 자동 검증을 실행해서 `valid_msg`가 관리자 화면에 기대한 대로 보이는지 확인한다.
- 필요하면 관리자 화면에서 사업자 검증 이력에 `failureCode`도 함께 노출한다.
- 실제 운영 데이터로 exact address 여러 건 중 상호명까지 같은 복수 케이스가 있는지 다시 확인한다.

## Mixed 데이터 재테스트 (하삼동 주소 + 해주파트너스 사업자정보)
- 테스트 시각: 2026-04-11 05:31 KST
- 입력 조합
  - storeName: 하삼동 커피
  - businessNumber: 1318645187
  - representativeName: 권오현
  - businessOpenDate: 2012-12-30
  - businessAddress: 경기도 안양시 만안구 만안로 35
  - businessPhone: 01063911426
- 결과
  - requestStatus: UNDER_REVIEW
  - businessVerificationStatus: AUTO_VERIFICATION_FAILED
  - mapVerificationStatus: VERIFIED
  - verifiedStoreName: 하삼동커피 안양만안구청점
- business 실패 이력
  - failureCode: NTS_VERIFICATION_FAILED
  - failureMessage: 확인할 수 없습니다. 사업자등록번호, 대표자명, 개업일자, 주소를 다시 확인해 주세요.
- map 성공 이력
  - queryText: 경기도 안양시 만안구 만안로 35
  - selectedExternalPlaceId: 2018589196
  - selectedRoadAddress: 경기 안양시 만안구 만안로 35
  - selectedJibunAddress: 경기 안양시 만안구 안양동 533-6

## 국세청 진위확인 요청 필드 정책 변경
- 국세청 `/validate` 요청에서 `b_adr`를 제거했다.
- 이제 국세청 자동 검증은 `사업자등록번호`, `대표자명`, `개업일자`만 전송한다.
- `NationalTaxServiceClient.verifyBusiness(...)` 시그니처를 3개 인자로 단순화했다.
- `OwnerApplicationService`의 자동 검증 호출부와 관련 테스트를 모두 새 시그니처로 맞췄다.
- 검증
  - `apps/backend` `./gradlew test` 통과

## 해주파트너스 실데이터 재검증
- 테스트 시각: 2026-04-11 05:47 KST
- 입력값
  - storeName: 주식회사 해주파트너스
  - businessNumber: 1318645187
  - representativeName: 권오현
  - businessOpenDate: 2012-12-30
  - businessAddress: 인천광역시 미추홀구 소성로 185번길 16-20
  - businessPhone: 01063911426
- 결과
  - requestStatus: UNDER_REVIEW
  - businessVerificationStatus: AUTO_VERIFIED
  - mapVerificationStatus: FAILED
- 해석
  - 국세청 진위확인은 사업자등록번호, 대표자명, 개업일자 기준으로 성공했다.
  - 카카오 exact address 검증은 실패했다.

## 혼합 성공 케이스 (해주파트너스 사업자정보 + 하삼동 커피 실영업정보)
- 테스트 시각: 2026-04-11 05:49 KST
- 입력값
  - storeName: 하삼동 커피
  - businessNumber: 1318645187
  - representativeName: 권오현
  - businessOpenDate: 2012-12-30
  - businessAddress: 경기도 안양시 만안구 만안로 35
  - businessPhone: 01063911426
- 결과
  - requestStatus: UNDER_REVIEW
  - businessVerificationStatus: AUTO_VERIFIED
  - mapVerificationStatus: VERIFIED
  - verifiedStoreName: 하삼동커피 안양만안구청점

## 매장등록신청 내역 초기화
- 요청에 따라 백엔드를 재시작해 H2 메모리 DB의 런타임 신청 데이터를 초기화했다.
- 재시작 후 관리자 신청 목록을 확인한 결과, 방금 테스트로 만든 신청들은 모두 삭제됐다.
- 현재 남아 있는 신청은 `DevDataInitializer`가 넣는 개발 시드 1건(`운영 확인용 토글 샵`)뿐이다.

## 로컬 MySQL 전환
- `application.yml` datasource 기본값을 H2 메모리 DB에서 로컬 MySQL로 변경했다.
  - url: `jdbc:mysql://127.0.0.1:3306/toggle...`
  - username: `root`
  - password: `1234`
  - driver: `com.mysql.cj.jdbc.Driver`
- 로컬 MySQL에 `toggle` 데이터베이스를 생성했다.
- 백엔드를 재기동했고 Hikari 로그에서 MySQL 커넥션이 실제로 붙는 것을 확인했다.
- 참고 이슈
  - 기존 MySQL 데이터의 `favorites.updated_at = 0000-00-00 00:00:00` 때문에 `ddl-auto=update` 과정에서 경고가 발생했다.
  - 서버 기동은 성공했지만, 기존 로컬 DB 데이터를 정리하거나 마이그레이션으로 보정하는 것이 안전하다.

## MySQL 스키마 재생성
- 요청에 따라 `spring.jpa.hibernate.ddl-auto`를 `update`에서 `create`로 변경했다.
- 백엔드를 재기동했고, 이번에는 기존 MySQL 데이터 영향을 받지 않고 스키마가 새로 생성되며 정상 기동하는 것을 확인했다.
- 현재 백엔드는 로컬 MySQL `toggle` 데이터베이스를 기준으로 동작한다.

## ddl-auto 복원
- 요청에 따라 `spring.jpa.hibernate.ddl-auto`를 다시 `update`로 복원했다.
- MySQL 연결 설정은 유지한다.

## application.yml 고정값 전환
- `application.yml`의 `${...}` 환경변수 참조를 모두 제거했다.
- JWT, MySQL, 업로드 경로, 국세청 API, 카카오 API 값을 전부 파일 안의 직접 값으로 고정했다.
- 환경변수 없이 `./gradlew bootRun`으로 백엔드를 기동했고, MySQL 연결까지 정상 동작하는 것을 확인했다.

## 환경변수 정리 확인
- 현재 셸 환경에서 Toggle 관련 env(`JWT_SECRET`, `SPRING_DATASOURCE_*`, `KAKAO_API_KEY`, `NATIONAL_TAX_SERVICE_KEY` 등)는 확인되지 않았다.
- `~/.zshrc`, `~/.zprofile`, `~/.bash_profile`에서도 관련 export는 발견되지 않았다.
- 즉 현재 프로젝트는 `application.yml` 고정값만 사용하는 상태다.

## 백엔드 설정 정책 고정
- 사용자 요청에 따라 백엔드 설정은 앞으로 `application.yml`의 직접 고정값만 사용하도록 규칙을 명시했다.
- `${...}` 환경변수 placeholder는 백엔드 config에 다시 도입하지 않는 정책으로 고정했다.
- 반영 파일
  - `.agents/agents.md`
  - `.agents/rules/backend-rules.md`
  - `handoff/qa/auth-test.md` (예전 env 기반 설명을 현재 정책에 맞게 정리)
