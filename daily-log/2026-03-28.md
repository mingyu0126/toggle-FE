# 2026-03-28

## 오늘 한 일
- Toggle 프로젝트의 DBML/ERD 구조를 리뷰하고 MVP 적합성, 제약조건, 인덱스, 네이밍 관점의 개선안을 정리했다.
- `toggle.md`를 기준으로 영업 상태 enum 기준을 `OPEN`, `BREAK_TIME`, `CLOSED`, `TEMP_CLOSED`, `EARLY_CLOSED`로 통일했다.
- `role`, `visibility`, `target_type`, `report_status`의 표준 enum 기준을 `toggle.md`와 ERD 문서에 명시했다.
- `.agents` 내부 규칙 문서와 스킬 문서의 상태 enum 불일치를 수정했다.
- `toggle_erd.md`의 `business_status`, `previous_status`, `new_status`에 동일한 enum 기준을 명시했다.
- 프로젝트 로컬 멀티에이전트 구조와 스킬 구성을 점검했다.
- `github-actions-cicd`, `backend-erd-review` 스킬이 추가된 것을 확인하고 폴더 구조 적합성을 검토했다.
- `github-actions-cicd`, `backend-erd-review` 스킬에 YAML frontmatter(`name`, `description`)를 추가했다.
- `handoff` 디렉토리의 역할과 `daily-log` 디렉토리의 분리 목적을 정리했다.
- 현재 프로젝트 구조에 맞는 GitHub Actions CI 초안(`.github/workflows/ci.yml`)을 추가했다.
- `.agents` 문서와 backend/qa workflow에 `backend-erd-review`, `github-actions-cicd` 스킬 연결을 반영했다.
- `apps/backend`에 Java 21 + Spring Boot 3.x + Gradle 기반의 최소 백엔드 골격을 추가했다.
- `toggle-FE` 프론트 프로젝트를 `apps/frontend`로 가져오고, 중첩 Git 저장소가 되지 않도록 루트 저장소에 편입 가능한 형태로 정리했다.
- 실제 프론트 코드 구조를 기준으로 `frontend-rules.md`를 재작성하고, 비어 있던 `build-frontend-feature.md`를 운영 가능한 워크플로우로 작성했다.
- 프론트 `status.js`, `StatusBadge`, `Pos`, `stores.mock`를 문서 표준 enum(`OPEN`, `BREAK_TIME`, `CLOSED`, `TEMP_CLOSED`, `EARLY_CLOSED`) 기준으로 정리했다.
- `favorite-store` 기준 PRD, API 계약서, QA 시나리오를 `handoff/pm`, `handoff/eng-review`, `handoff/qa`에 작성했다.
- 카카오 장소 식별 전략을 반영해 `stores.external_source`, `stores.external_place_id` 기준 문서와 handoff 설계를 보강했다.
- `apps/backend`에 `com.toggle` 기준 패키지 구조와 `Store resolve`, `Favorite` API, 엔티티, 리포지토리, 서비스, 공통 예외/응답 구조를 추가했다.
- H2 test profile과 MockMvc 기반 기본 API 테스트를 추가했다.
- 백엔드 Gradle wrapper를 추가하고 CI 실행 기준을 `./gradlew clean test`로 정식화했다.
- 프론트 즐겨찾기 흐름을 `resolve -> add/remove/list` 백엔드 API 기준으로 연결하고, 모바일/웹 즐겨찾기 화면과 상세 하트 버튼을 서버 연동 방식으로 전환했다.
- 프론트 개발용 `.env.example`을 추가해 카카오 키, 백엔드 주소, 임시 개발 사용자 ID 기준을 정리했다.

## 변경한 파일
- `/Users/gwon-ohhyeon/Desktop/toggle/.agents/rules/backend-rules.md`
- `/Users/gwon-ohhyeon/Desktop/toggle/.agents/rules/frontend-rules.md`
- `/Users/gwon-ohhyeon/Desktop/toggle/.agents/rules/qa-rules.md`
- `/Users/gwon-ohhyeon/Desktop/toggle/.agents/skills/design-api-contract/SKILL.md`
- `/Users/gwon-ohhyeon/Desktop/toggle/.agents/skills/generate-test-scenarios/SKILL.md`
- `/Users/gwon-ohhyeon/Desktop/toggle/.agents/skills/github-actions-cicd/SKILL.md`
- `/Users/gwon-ohhyeon/Desktop/toggle/.agents/skills/backend-erd-review/SKILL.md`
- `/Users/gwon-ohhyeon/Desktop/toggle/docs/architecture/toggle_erd.md`

## 메모
- `handoff`는 역할 간 인계 산출물을 두는 폴더이고, `daily-log`는 개인 작업 일지 용도로 분리하는 것이 적합하다.
- `gstack` 계열 스킬은 Toggle 전용 스킬이라기보다 리뷰, QA, 배포 검증을 돕는 범용 실무 보조 스킬로 보는 것이 맞다.
- 현재 `.agents/workflows/build-frontend-feature.md`는 비어 있어서 추후 보완이 필요하다.

## 다음 할 일
- `agents.md` 또는 workflow 문서에 `github-actions-cicd`, `backend-erd-review` 스킬의 사용 시점을 연결한다.
- 비어 있는 `build-frontend-feature.md`를 프로젝트 규칙에 맞게 작성한다.
- 필요하면 `.github/workflows/ci.yml` 초안을 만든다.
- `role`, `visibility`, `report_status`, `target_type` 같은 다른 enum성 문자열도 문서 전체에서 표준화할지 결정한다.
- CI가 실제 backend build/test까지 수행하도록 `apps/backend`에 Gradle 기반 Spring Boot 프로젝트를 연결한다.
- 로컬 Gradle 환경 문제를 정리하거나 wrapper를 도입해 backend 테스트를 로컬에서도 실행 가능하게 만든다.
- `apps/frontend`를 현재 Toggle 루트 구조와 맞게 검토하고 필요한 frontend workflow 규칙을 보강한다.
- 프론트의 현재 status 상수(`BREAK`, `EARLY_CLOSE` 등)와 문서 표준 enum(`BREAK_TIME`, `EARLY_CLOSED`)을 실제 코드에서도 맞춘다.
- `FavoritesWeb` 등 일부 화면이 아직 mock/localStorage 기준이 섞여 있어서 즐겨찾기 UX를 실제 API 연동 형태로 정리한다.
- 로컬 Gradle 네이티브 라이브러리 문제를 해결해서 backend 테스트를 실제 실행 검증한다.

## 블로커
- 아직 실제 앱 코드와 GitHub Actions workflow 파일이 없는 상태라서 CI/CD 스킬은 설계 중심으로만 검토 가능하다.
