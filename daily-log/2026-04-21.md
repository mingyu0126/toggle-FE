# 2026-04-21

## 브랜치 마무리 및 검증
- 현재 `ohhyeon` 브랜치의 누적 변경분을 정리하고 `main` 병합 준비를 진행했다.
- 프론트 린트에서 남아 있던 실제 에러를 `ImageCarousel`, `Admin`, `Landing`, `LandingWeb`, `PublicWeb` 기준으로 정리했다.
- `PublicWeb`은 unused state와 검색 중심 참조가 꼬여 있던 부분을 정리해 다시 lint 통과 상태로 맞췄다.

## 검증
- `apps/backend`: `./gradlew test` 통과
- `apps/frontend`: `npm run build` 통과
- `apps/frontend`: `npm run lint` 통과
  - 기존 경고는 남아 있으나 에러는 없다.

## 프론트 문구 정리
- 랜딩 페이지에서 `공공기관 혼잡도` 기능 카드와 관련 설명 문구를 제거했다.
- 로그인 화면에서 `+2K` 사회적 증명 카피와 `POS Dashboard 접근` 버튼 문구를 정리하고, 점주 표기에서 `POS`를 뺐다.
- 브라우저 프리뷰에서 반영 결과를 확인했다.

## 로고 교체
- `/`, `/web`, `/loginweb`, `/signupweb`의 상단 브랜드 표기를 공통 PNG 로고로 교체했다.
- 로고 이미지에 `object-fit: contain`과 명시적인 `width`/`height`를 적용해 찌그러짐을 막았다.
- `signupweb`의 텍스트 로고도 동일한 이미지 자산으로 맞췄다.
